// TODO: Bit faster
class ModerniAliasDurations {
  static const animation = Duration(milliseconds: 300);
  static const slowAnimation = Duration(milliseconds: 600);
  static const verySlowAnimation = Duration(milliseconds: 1200);
  static const fastAnimation = Duration(milliseconds: 150);
  static const veryFastAnimation = Duration(milliseconds: 75);
}
